<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When checked -->
    <item android:color="@color/primaryColor" android:state_checked="true" />
    <!-- When unchecked -->
    <item android:color="@color/primaryColor" android:state_checked="false" />
</selector>